.row
  .large-12.columns
    - flashes = {:notice => '', :alert => '', :error => 'alert', :secondary => 'secondary'}
    - flashes.each do |key, value|
      - if flash[key]
        .alert-box{:class => "alert-box #{value}", :data => { :alert => "" } }
          = flash[key]
          = link_to '&times;'.html_safe, '', :class => 'close'
